# API
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
python-multipart==0.0.9

# CV — detection backends
opencv-python-headless==4.9.0.80
numpy==2.1.0

# YOLOv8 + ByteTrack (GPU-optional; falls back to CPU automatically)
ultralytics==8.2.18          # YOLOv8 — includes YOLO class and model weights download
lap==0.5.12                  # Linear assignment solver required by ByteTrack
supervision==0.21.0          # ByteTrack tracker + annotation utilities

# Utilities
requests==2.32.3
rich==13.7.1

# Dev / CI
pytest==8.2.0
httpx==0.27.0
ruff==0.4.4
black==24.4.2
streamlit>=1.35.0
plotly>=5.22.0
